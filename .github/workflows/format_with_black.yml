name: Format with Black

on: [push]

jobs:
  format_with_black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2
      - uses: psf/black@stable
        with:
          options: "./"
      - uses: EndBug/add-and-commit@7
